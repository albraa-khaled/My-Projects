how run thew code:
1. put the dataset file path in the variable that named (file_path ).
2.change the number of (min_support=minimum support)and(max_scans=the number of scans)if you want.
Defaults:
max_scans=3
min_support=0.4
3.run and start debugging and then the results will be printed in terminal.